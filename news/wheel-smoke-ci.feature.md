Add `wheel-smoke.yml` workflow that builds a fresh sdist + wheel and exercises the published-install path users actually take with `pip install pycoraza`. The smoke job spawns a clean consumer venv on a separate runner, installs the wheel with `[flask,fastapi,starlette]` extras, boots each adapter under gunicorn / uvicorn with `PYCORAZA_WAF=on PYCORAZA_MODE=block`, and probes a 200/200/403/{200|403}/403 matrix across benign, XSS, and SQLi shapes — closing the gap that editable-install CI never caught packaging regressions. Reusable as `bash bench/wheel_smoke.sh <wheel>` for local debugging.
