Add a top-level `NOTICE` file and `THIRD_PARTY_LICENSES/` directory shipping the upstream Apache-2.0 license texts for libcoraza, Coraza, and OWASP CRS — components linked or bundled into the wheel. `pyproject.toml` declares them via `license-files = ["LICENSE", "NOTICE", "THIRD_PARTY_LICENSES/*"]` so the wheel's `pycoraza-*.dist-info/licenses/` includes the full attribution set required by Apache-2.0 §4(d).
