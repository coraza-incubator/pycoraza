Add `Transaction.is_rule_engine_off()`, `is_request_body_accessible()`, `is_response_body_accessible()`, and `reset()` predicates as forward-looking shape parity with the planned upstream libcoraza API. Until the corresponding `coraza_*` C exports land in libcoraza, every method raises `NotImplementedError` with a clear message — callers fall back to the existing pipeline (run the WAF) or to creating a new transaction (no in-place reuse). No behavior changes to existing methods.
